# Orca MCP - Python Dependencies
# Pinned to working versions (2026-02-14) to prevent Railway build breakage

# Core dependencies
pandas==2.2.3
numpy==1.26.4
requests==2.32.5
httpx==0.28.1  # Async HTTP client for concurrent API calls (7-18x speedup)

# LLM providers for FallbackLLMClient (orca_query routing)
# Routing order: Gemini Flash -> OpenAI Mini -> Grok -> Haiku
anthropic==0.50.0
openai==1.76.0
google-genai==1.12.1

# BigQuery and Cloud Storage
google-cloud-bigquery==3.27.0
google-cloud-storage==2.19.0
pyarrow==22.0.0
db-dtypes==1.4.4

# Redis caching
redis==6.4.0

# HTTP API server (Railway deployment - legacy REST API)
flask==2.3.3
gunicorn>=21.0.0

# MCP SDK and SSE transport (for Claude Desktop remote connections)
mcp==1.25.0
fastapi==0.109.2  # Modern async web framework with auto-docs
starlette==0.36.3
uvicorn==0.40.0
sse-starlette==1.8.2
